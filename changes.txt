# make defualt shell for new users to zsh
/etc/default/useradd
     SHELL=/bin/zsh
     
#change current users shell to zsh
chsh <username> -s /bin/zsh

# remove systemd-bootloader timeout 
/boot/loader/loader.conf
     timeout=0

# ssh
sudo systemctl enable --now sshd
sudo systemctl status sshd

# Hyper-V

     # linux guest enable service:
     hv_fcopy_daemon.service, hv_kvp_daemon.service and hv_vss_daemon.service
          sudo systemctl start <service>
          sudo systemctl enable <service>

     # windows hyper-v host 
     enable-VMIntegrationService -VMName "linuxbox" -Name "Guest Service Interface"
     PS C:\Users\camidon> Get-VMIntegrationService -VMName "linuxbox"
      

# SMB Shares
  mount -t cifs [Network Path with forward slashes] mountpoint -o user=[user you wish to authenticate as],ip=[host IP/UNC]

