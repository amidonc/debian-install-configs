/etc/login.def
    LOGIN_RETRIES=1
    LOGIN_TIMEOUT=30

/etc/systemd/system/getty.target.wants/getty@tty1.service
  ExecStart=-sbin/agetty -o '-p -- \\u' --noissue --nohostname - $TERM

/etc/default/useradd
     SHELL=/bin/zsh

chsh <username> -s /bin/zsh

