man-db
man-pages
zsh
grml-zsh-config
base-devel
exa
vim
curl
git

go # needed for yay
yay
  cd /opt
  sudo git clone https://aur.archlinux.org/yay.git  
  sudo chown -R <username>:users ./yay
  cd yay
  makepkg -si


